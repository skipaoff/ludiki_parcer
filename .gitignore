# secrets — API keys live only in Windows Credential Manager, never in files
.env
.env.*
!.env.example
*.pem
*.key
secrets*

# local settings — config.example.toml is the committed template
config.toml

# local runtime data
*.db
*.sqlite*
data/
logs/
*.jsonl

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# web
node_modules/
dist/

# os
.DS_Store
